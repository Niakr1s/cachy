# Desktop PC — heavy setup (NVIDIA GPU, VMs, emulators...).
# Starts from the shared 'core' profile, then adds desktop-only tasks.

%include core

nvidia
virtualization
hibernation off
ai-packages
emulators
